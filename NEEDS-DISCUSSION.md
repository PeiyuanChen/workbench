# 待调查 / 待处理点（M2 走查遗留）

> 用途：记录 M2 走查中发现、但不在当前批次处理的点。处理时新建规格/任务，不要直接改。
> 已完成项不再保留于此（A 类两个同步 bug、C 类五项 UX 均已落地并验证）。
> 最后更新：2026-09-17。

## #6 待办同时有 DUE 与执行块时，日历显示成两条（搁置，需先厘清语义）

- 现象：一条待办同时设置「截止 DUE」和「执行块 DTSTART+DURATION」后，
  日历月视图出现两条：▸ 蓝色执行块（带时间）+ ⚑ 红色截止旗。
  例：`回顾之前RAG遇到的问题`，执行块 11:00–12:00、DUE 当天 → 当天格子两条。
- 当前实现（M2 既定行为，非刷新 bug）：
  `backend/app/service/calendar_service.py:114-146` 的 month_items 对每个待办，
  执行块（dtstart+duration）和截止（due）是两个独立 if、各自 append 一条聚合项；
  四象限 `timeSpans` 也是两个标记并列。改刷新逻辑不会改变“两条”，数据层本就产出两条。
- 搁置原因（用户 2026-09-17）：用户重新思考 DUE 与 DEADLINE 的语义；且当前日历
  不按小时展示，同事项两条显得重复。是否/如何合并取决于先厘清语义，不在 M2 收尾前仓促定。
- 需要先想清楚：
  1. 本产品里 DUE 表达什么——硬性截止（deadline）还是“希望当天看到的提醒”？
     与“执行块时间段”应互斥、并列还是从属？
  2. 日历未来若按小时展示（M3 拖拽改时间的前提），块占时间轴、旗标日期，本就是两个信息，
     现在合并会不会限制 M3。
  3. 若仍要合并，候选口径：执行块覆盖日 == DUE 日时合并为一条（▸ 块 + ⚑ 角标），
     不在同一天则两处各显示。改动在 month_items 聚合层，需配后端 pytest
     （同日合并 / 跨日两条 / 仅 due / 仅块 四例）。
- 状态：**暂缓，语义厘清后再议；不阻塞 M2 收尾。**

## #9 看板状态 + 细粒度进度（M3 候选，涉及数据模型）

- 需求（用户 2026-09-16 提出）：待办是否需要 todo / ongoing / done 看板状态；
  进度是否需要细粒度表达，例如 75%、或工时 3hr/10hr（总计划 10 小时、已进行 3 小时）。
- 数据模型考量（讨论前需拍板）：
  - ongoing 对应 iCalendar 标准 `STATUS:IN-PROCESS`（后端 filter 已能识别 IN-PROCESS 为活跃态，
    见 todo_service.filter_scope 测试），加这一态不破坏外部日历可读性。
  - “工时预算 / 已耗工时 / 百分比”没有标准 ICS 字段：要么走 X- 自定义扩展属性
    （外部日历会忽略、但自家读写需契约约束），要么存独立元数据文件（偏离“一切在 ics/md”）。
    与“数据主权、开放格式、外部日历可读”底线有张力，需要权衡。
  - 与现有父子任务进度（children_done/children_total，服务端权威）的关系要厘清：
    百分比是手工填还是由子任务/工时自动算？
- 状态：**进 M3 规划，与时间线 LLM、拖拽改时间等一起在新 SPEC 讨论；不塞进 M2。**
