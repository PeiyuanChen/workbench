# 个人工作台 · 技术方案（v1 待审）

> 配套架构图：`工作台-技术架构图.html`（浏览器打开）
> 数据样例：`data-samples/`（含可跑的渲染/校验脚本）

## 一、设计原则（宪法，优先级最高）

1. **数据主权**：所有数据用开放、人类可读格式（Markdown + iCalendar），存在你自己的设备/服务器。软件只是一层"皮"，可随时替换、重写、停摆而数据无损。
2. **视图 ≠ 数据**：四象限、时间线不是独立数据结构，是从基础数据（待办标签、Markdown 月份文件）现场计算出来的视图。换工具只换视图，不动数据。
3. **一套后端，多端复用**：Web 先行，iOS/Android 后期接同一套接口；数据逻辑只在后端/数据层写一次。
4. **AI 辅助而非依赖**：AI 负责一次性迁入/迁出转换与总结；即使没有 AI，数据也能被记事本、Obsidian、Google 日历等通用工具直接打开。
5. **离线优先（local-first）**：断网可用，同步是加分项不是前提。

## 二、分层架构

| 层 | 职责 | 选型（可替换） |
|---|---|---|
| ① 客户端/视图 | 四象限、待办、日历、备忘、时间线界面 | Web：响应式 PWA（先做）；手机：PWA/原生壳/CalDAV 客户端（后做） |
| ② 协议/接口 | 中立契约 | **CalDAV/WebDAV**（待办+日历，RFC 5545/4791）+ **REST/文件同步**（Markdown、时间线聚合） |
| ③ 后端服务 | 薄：读写开放文件、生成视图 JSON，不持有业务数据 | CalDAV 服务器（**Radicale** 最简，或 Vikunja）+ 工作台 API（**FastAPI** 或 Node，均易替换） |
| ④ 数据层 | 开放格式文件 | `.ics`（待办/日历）、`notes/*.md`（备忘）、`timeline/YYYY/MM.md`（时间线），Git 版本化 |

关键：**③ 后端不建私有数据库表**，它读写的就是 ④ 的开放文件。这样后端哪怕整个删掉，数据依然完整可用。

## 三、数据模型（见 data-samples/ 真实样例）

```
data/
├─ calendar.ics              # 待办 VTODO + 事件 VEVENT（CalDAV 管理）
│   待办字段：SUMMARY, DUE?(可选), CATEGORIES,
│            X-IMPORTANT(TRUE/FALSE), X-URGENT(TRUE/FALSE), STATUS
│   无 DUE 的待办 = "收件箱/最近要做"（RFC 5545 标准语义）
├─ notes/
│   └─ 2026-09-03-标题.md     # frontmatter(id,tags,related) + Markdown 正文
└─ timeline/
    └─ 2026/
        └─ 03.md             # 月度主线/决策/回顾，前端按月聚合
```

**四象限归位逻辑**（纯计算）：
- 重要且紧急 = `X-IMPORTANT:TRUE` + `X-URGENT:TRUE` → 立即做
- 重要不紧急 = T + F → 计划做
- 紧急不重要 = F + T → 快做/委托
- 不重要不紧急 = F + F → 有空再做
- 有没有 `DUE` 只决定它进"已排期"还是"未排期/收件箱"，与象限正交。

## 四、部署形态（由简到全，可渐进）

- **阶段 0（零服务）**：纯文件夹 + 记事本/Obsidian 用 Markdown，待办用任意 CalDAV 客户端连本地 .ics。已能跑。
- **阶段 1（本机服务）**：本机起 Radicale（CalDAV）+ FastAPI（工作台 API），Web 工作台读 `D:\tasks\workbench\data`。
- **阶段 2（自托管）**：部署到你的小服务器/NAS，手机和笔记本通过 CalDAV + REST 同步；数据仍是服务器上的开放文件 + Git。
- **手机端**：Web PWA 先在手机浏览器"添加到主屏"即可用；需要原生体验再包壳，接口不变。

## 五、分阶段计划（建议）

- **M1 数据层 + 只读原型**：定稿 .ics/.md 约定（data-samples 已验证），写 FastAPI 读取本地文件夹 → 返回四象限/待办/时间线 JSON；一个纯静态 Web 页渲染。目标：能看到自己真实数据。
- **M2 可写 + CalDAV**：接入 Radicale，待办可增删改、打象限标签、设/不设时间；备忘 Markdown 可编辑保存。
- **M3 时间线 + 导入**：按月时间线视图（含从待办/日历自动汇总当月事件草稿）；用 AI 脚本把 TickTick/旧笔记一次性迁入 .md/.ics（可校验）。
- **M4 多端同步**：自托管 + PWA 手机端 + 可选 Google 日历双向（.ics 订阅/CalDAV）。

## 六、待你拍板的点

1. 后端语言偏好：Python(FastAPI) 还是 Node？（你主力 Java，Spring Boot 也行，只是读 Markdown/起 CalDAV Python 更轻）
2. CalDAV 服务器：先用最轻的 **Radicale**（单文件 .ics），还是直接上 **Vikunja**（自带 Web/客户端、功能多）？建议 M1-M2 用 Radicale 跑通，需要团队/复杂功能再换。
3. 数据根目录：就定 `D:\tasks\workbench\data\` ？
4. M1 先做"只读能看"还是直接"可增删改"？建议先只读看到效果，再做写。
