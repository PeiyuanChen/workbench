# 个人工作台 · 概要设计（v1 · 已评审待固化）

> 状态：用户已评审整体方向，可固化。配套：`工作台-技术架构图.html`、`data-samples/`。
> 日期：2026-09-03

## 1. 项目目标

一个本地优先（local-first）的**个人**工作台，聚合：艾森豪威尔**四象限**、**待办**、**备忘 note**、**月度时间线**（回溯"某月主要做了什么"）、**日历**。
Web 先行（PWA），未来 iOS/Android 复用同一后端。最高约束：**数据主权**——数据用开放格式、用户永久持有，软件只是一层可替换的皮。

## 2. 设计宪法（优先级最高）

1. **数据主权**：所有业务数据 = Markdown + iCalendar(.ics) 纯文本，存用户自己的设备/服务器；离线可用。
2. **视图 ≠ 数据**：四象限、时间线是从基础数据现场计算的视图，不是私有数据结构。换工具只换视图。
3. **一套后端多端复用**：Web 先行，手机端接同一套接口，数据逻辑只写一次。
4. **AI 辅助而非依赖**：AI 做一次性迁入/迁出与总结；没有 AI，数据也能被记事本/通用工具直接打开。
5. **离线优先**：断网可用，同步是加分项。
6. **数据格式变更须用户单独确认**（它是地基）。

## 3. 架构分层（模块化单体）

| 层 | 职责 | 选型 |
|---|---|---|
| ① 客户端/视图 | 四象限、待办、日历、备忘、时间线 UI | Web 响应式 PWA（先做）；手机端后做 |
| ② 协议/接口 | 中立契约 | CalDAV/WebDAV（待办+日历）+ REST/文件同步（Markdown、时间线） |
| ③ 后端服务 | 薄：读写开放文件、生成视图 JSON，**不持有业务数据、不建私有库表** | 工作台 API（**建议 Python/FastAPI**）+ CalDAV 服务器 **Radicale**（第三方现成） |
| ④ 数据层 | 开放格式文件 + Git 版本化 | `.ics`、`notes/*.md`、`timeline/YYYY/MM.md` |

**服务进程**：我们只开发 **1 个**（workbench-api）。Radicale 是第三方小程序，M1 可不启动，M2 起用于待办/日历编辑与多端同步。运行时最多 2 进程，不做微服务。

## 4. Package 划分与依赖方向（单向）

```
workbench/
├─ api/          # HTTP 路由：接请求、返 JSON；不含业务逻辑
│   ├─ routes_todos.py
│   ├─ routes_events.py
│   ├─ routes_notes.py
│   └─ routes_timeline.py
├─ service/      # 业务逻辑：四象限归位、时间线按月聚合、收件箱过滤
├─ datastore/    # 唯一碰文件系统：解析/生成 .ics、读写 .md
│   ├─ ics_store.py   # icalendar 库
│   └─ md_store.py    # frontmatter
├─ core/         # 配置、路径解析、日志、数据契约常量
└─ web/          # 前端 PWA 静态文件
```
依赖方向：`api → service → datastore → 文件系统`；`core` 被各层引用。下层不知道上层。datastore 将来可从"直接读写文件"换成"调 CalDAV"，上两层不变。

## 5. 数据契约（详见 data-samples/，已用真实 icalendar 库校验）

```
data/users/me/
├─ calendar.ics            # 待办 VTODO + 事件 VEVENT
├─ notes/                  # 备忘：一篇一 .md（frontmatter: id/tags/related）
│  └─ YYYY-MM-DD-标题.md
└─ timeline/YYYY/MM.md     # 月度叙述：主线/决策/回顾（本身就是按月命名的 note）
```
- 待办字段：`SUMMARY, DUE?(可选), CATEGORIES, PRIORITY, STATUS, X-IMPORTANT, X-URGENT`。
- **无 DUE 的待办 = 收件箱/最近要做**（RFC 5545 语义：关联到每天直到完成）；待办唯一必填 `UID, DTSTAMP`。
- 四象限 = `X-IMPORTANT` × `X-URGENT` 两标签现算；与有无时间正交。
- 目录起步即用 `data/users/me/`（见 §8 多用户扩展缝）。

## 6. API 清单（功能级）

| 方法 | 路径 | 功能 | 里程碑 |
|---|---|---|---|
| GET | `/api/health` | 健康检查 | M1 |
| GET | `/api/todos` | 待办（可按象限/未排期/状态过滤） | M1 |
| GET | `/api/todos/{id}` | 单条待办 | M1 |
| GET | `/api/events?month=YYYY-MM` | 某月日历事件 | M1 |
| GET | `/api/timeline/{year}/{month}` | 月时间线（聚合待办+事件+md） | M1 |
| GET | `/api/notes`、`/api/notes/{id}` | 备忘列表/单篇 | M1 |
| POST/PATCH/DELETE | `/api/todos` | 待办增改删（打象限、设/清时间） | M2 |
| POST/PATCH/DELETE | `/api/events` | 日历事件增改删 | M2 |
| PUT/POST | `/api/notes` | 备忘/月度叙述增改 | M2 |
| —— | CalDAV 同步 | Radicale 直接提供（待办/事件的第二条写入路径），不在此 API 内 | M2/M4 |

说明：待办/事件有两条合法写入路径——① Web 界面→REST→写 .ics；② Google/Apple 日历等→CalDAV→Radicale→同一份 .ics。notes/timeline 是 Markdown，CalDAV 管不着，由 REST 独占。timeline 的 GET 是聚合视图；写月度叙述走 notes 接口编辑该月 md。

## 7. 单用户设计（当前）

明确**单用户**：不做注册/登录/权限/多租户；API 不鉴权（或仅一个本地简单 token 防局域网误访问）。local-first 个人工具，非 SaaS。

## 8. 多用户扩展缝（现在零成本预留，避免将来返工）

将来若要"每人一个私有工作台"（**隔离式**多用户），靠两条现在就守的纪律平滑扩展、无需重构：
1. **数据路径统一解析**：所有文件访问走 `core` 的一个 `data_root()` 函数，代码中**禁止写死路径**；将来改为 `data_root(current_user)` → `data/users/{uid}/`。
2. **目录结构预留**：起步即 `data/users/me/...`，将来 `me` 换成用户 id。
届时只需：加认证层 + 改路径函数 + 查询加用户过滤 + Radicale 每用户一账户（原生支持）。业务逻辑（四象限/时间线）不变。
注：**协作式**多用户（共享日历/待办、看到彼此）属另一量级，明确不做、不预埋。

## 9. 里程碑

- **M1 数据层 + 只读原型**：定稿 .ics/.md 契约；FastAPI 读本地文件夹返 JSON；静态 Web 渲染四象限/待办/日历/时间线/备忘。验收：能看到自己的真实数据。
- **M2 可写 + CalDAV**：接入 Radicale；待办/事件增删改、象限标签、设/清时间；备忘编辑保存。
- **M3 时间线 + 迁入**：时间线视图（自动汇总当月草稿）；AI 脚本把 TickTick/旧笔记迁入 .md/.ics（可校验）。
- **M4 多端同步**：自托管 + PWA 手机端 + 可选 Google 日历双向。

## 10. 已拍板项（2026-09-03）

- [x] **后端语言：Python + FastAPI**（读文件/解析.ics/起服务最省力；数据层独立，选型可后悔）。
- [x] **CalDAV 服务器：Radicale**（M1 可不启动，M2 起用于编辑/多端同步；复杂需求再评估 Vikunja）。
- [x] **数据根目录：`D:\tasks\workbench\data\`**（内部按 `data/users/me/...` 组织）。
- [x] **前端框架：Vue 3（Composition API + `<script setup>`）+ Vite + TypeScript + Tailwind CSS**；状态 Pinia；PWA 用 vite-plugin-pwa；组件库按需（Naive UI / Element Plus），拖拽用 VueDraggable。选定理由：后端出身单人自审、前端复杂度不高（渲染视图 JSON）、中文资料丰富、Claude Code 生成可靠。
- [x] **先做无逻辑静态原型**（假数据、纯 HTML/CSS 定布局/交互），审满意后再用 Vue 落地 M1。

## 11. 工具分工

- **Hermes（当前）**：方案/选型调研、概念解释、文档固化、评审组织、生成数据契约与样例、跨会话记忆。
- **Claude Code（本地）**：M1 起的具体设计/编码/测试/重构，在 `D:\tasks\workbench` 项目目录内，用 CLAUDE.md 加载本设计作为上下文。

## 12. 原型 v2 评审后的补充设计（2026-09-03，v1.1）

### 12.1 导航与"收件箱"
- 导航为 5 个视图：**四象限 / 待办 / 日历 / 时间线 / 备忘**。四象限与待办是**同一批 todo 的两个视图**（前者看优先级，后者看清单），不重复建模。
- 不设"收件箱"独立 tab；未排期待办作为待办视图里的**分区**"📥 未排期·最近要做"（命名可再定：未排期/待整理/最近要做）。
- "＋快速记录"输入框**在四象限、待办、备忘**三个视图出现（待办/四象限记的是一件事，备忘记的是一条笔记）；时间线、日历不显示。日历当前**只读展示**事件，暂不做新建入口。
- 未排期待办分区命名为"未排期/未归类"含义，**界面不出现"收件箱"字样**（该词仅作为 GTD 概念，不对用户展示）。

### 12.2 复杂事项：父子任务（iCalendar RELATED-TO）
- 采用**父子任务**范式（对齐 Todoist/滴答/Apple 提醒事项/Jira）：复杂事项 = 1 个父 VTODO + N 个子 VTODO。
- 子任务用 `RELATED-TO;RELTYPE=CHILD:<父UID>` 关联（RFC 5545 标准，CalDAV 客户端可识别）。
- 子任务是**完整待办**：可独立设时间、打象限标签、勾选；父任务进度 = 子任务完成率（自动汇总，如 2/3）。
- **默认两级**（父+子），不做无限层级。
- 轻量并列清单（如买东西）不专门建模——在待办**描述里写 Markdown `- [ ]`** 即可。

### 12.3 待办的三类时间与日历展示
一个待办支持渐进式、全部可选的时间字段：
| 时间 | 字段 | 日历展示 |
|---|---|---|
| 记录时间（自动） | `CREATED`/`DTSTAMP` | 不显示 |
| 截止 deadline（时间点，不占时长） | `DUE` | ⚑ 红色小旗/角标 |
| 执行时间块（可跨小时/多天） | `DTSTART`+`DURATION` | ▸ 色块；跨多天 `DURATION` 横跨多日 |
- 阶段：无时间（未排期）→ 设 DUE（有 deadline）→ 设 DTSTART+DURATION（排进日历）。
- 可同时有 deadline 与执行块。全天用 `VALUE=DATE`。
- 日历只读聚合展示：执行块画成色块、截止画成小旗；无时间待办不上日历。

### 12.4 时间线 = LLM 生成（产物仍为 Markdown）
- 范围：**月（默认）/季/半年/年/自定义**。
- 逻辑：打开范围 → 若 `timeline/` 已有对应 .md 则直接展示；否则把该范围的待办(完成/进行)+事件+备忘喂给 LLM，用**固定模板 prompt** 生成，**落盘为 .md** 再展示。
- 生成后用户可手动编辑该 md；可点"重新生成"。prompt 模板存配置文件。
- 新增接口：`POST /api/timeline/generate?range=2026-03`（触发 LLM 生成）；GET 仍只读已生成的 md。

### 12.5 补充后的 API（在 §6 基础上）
| 方法 | 路径 | 功能 | 里程碑 |
|---|---|---|---|
| GET | `/api/todos?view=quadrant\|list&unscheduled=&status=` | 待办（支持四象限/清单视图、未排期过滤）；返回含子任务层级与父进度 | M1 |
| POST/PATCH/DELETE | `/api/todos` | 待办增改删；支持设 `parent`（建子任务）、DUE、DTSTART+DURATION、象限标签 | M2 |
| GET | `/api/events?month=` | 日历：聚合事件 + 待办执行块 + 截止标记 | M1/M2 |
| POST | `/api/timeline/generate?range=` | 触发 LLM 生成时间线 md | M3 |
